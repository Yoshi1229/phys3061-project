# Phys3061 Final Project (The Kirkwood Gaps)
Run `py sim1.py` to start the simulation. Default parameters set to include Saturn with time frame 2.5Myr (`t_end`) and time step 0.25yr (`dt`), with 10000 asteroids. (May take many hours to run)

Change `muS` to `0.0` to exclude Saturn. 
